{
  "name": "aqworlds-character-api",
  "version": "1.0.0",
  "description": "Small API that extracts the AQWorlds charpage character viewer and equipped gear.",
  "main": "src/server.js",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "install:browsers": "playwright install chromium",
    "check": "node --check src/server.js && node --check src/render-gif.mjs && node --check src/custom-swf.js"
  },
  "dependencies": {
    "playwright": "1.52.0"
  }
}
